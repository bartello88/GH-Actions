on:
  workflow_call:
    inputs:
      input_name:
        required: True
        type: string

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4
      - name: Print input
        run:
          echo ${{ inputs.input_name }}


