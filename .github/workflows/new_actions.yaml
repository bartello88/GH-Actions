name: First Actions - runs manually
on: 
    workflow_dispatch:
    push:
        paths:
            - "app.py"
        paths-ignore: 
            - "main.*"

jobs:
    first-action:
        runs-on: ubuntu-latest
        steps:
        - name: Getting the code
          uses: actions/checkout@v4
        - name: Run the code
          run: |
            echo "Workflow dziala"
        
  
  
