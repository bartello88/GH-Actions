name: First Actions
on: 
    workflow_dispatch:

jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
        - name: Getting the code
          uses: actions/checkout@v4
        - name: Run the code
          run: |
            python app.py
        
  
  
