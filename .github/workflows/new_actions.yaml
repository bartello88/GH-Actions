name: First Actions
on: 
    workflow_dispatch:

jobs:
    first-action:
        runs-on: ubuntu-latest
        steps:
        - name: Getting the code
          uses: actions/checkout@v4
        - name: Run the code
          run: |
            echo "Workflow dziala"
        
  
  
